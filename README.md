# NoteFlow API

API en desarrollo para gestión de usuarios y notas personales.

## Tecnologías
- FastAPI
- MongoDB
- Pydantic

## Estado
Proyecto en desarrollo. Actualmente contiene la estructura base de rutas y schemas.

## Ejecutar
```bash
pip install -r requirements.txt
python -m uvicorn app.main:app --reload
